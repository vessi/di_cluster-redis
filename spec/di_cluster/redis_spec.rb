require 'spec_helper'

describe DiCluster::Redis do
  it 'has a version number' do
    expect(DiCluster::Redis::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
